[package]
name = "zkbluff"
version = "0.1.0"
edition = "2024_07"

[dependencies]
starknet = ">=2.6.0"

[[target.starknet-contract]]
sierra = true
casm = true

[dev-dependencies]
snforge_std = { git = "https://github.com/foundry-rs/starknet-foundry", tag = "v0.24.0" }

[scripts]
test = "snforge test"
